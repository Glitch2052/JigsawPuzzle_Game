using UnityEngine;

namespace Unity.Advertisement.IosSupport.Components
{
//     public sealed class ContextScreenView
//     {
//         public static void RequestAuthorizationTrackingFromUser()
//         {
// #if UNITY_IOS
//             // check with iOS to see if the user has accepted or declined tracking
//             var status = ATTrackingStatusBinding.GetAuthorizationTrackingStatus();
//             Debug.Log($"ATT Status is {status}");
//             if (status == ATTrackingStatusBinding.AuthorizationTrackingStatus.NOT_DETERMINED)
//             {
//                 Debug.Log("Unity iOS Support: Requesting iOS App Tracking Transparency native dialog.");
//                 ATTrackingStatusBinding.RequestAuthorizationTracking();
//             }
// #else
//             Debug.Log("Unity iOS Support: App Tracking Transparency status not checked, because the platform is not iOS.");
// #endif
//         }
//     }
}
